## option-tree

  - next - selNext
  - prev - selPrev
  - select(option)
  - active() -> any
  - isActive(any) -> bool

  - options() -> array
  - value() -> array

  - setQuery(string)
  - setOptions()
  - setValue()

  # action implementation also has
  - setActions(array of fns)
  - actions()

  # maybe
  - filter() -> function
  - setFilter(fn)
